const header = {
    display: 'flex',
    alignItems: 'center',
};

const widget = {
    marginBottom: 'inc50',
};

const title = {
    marginLeft: 'inc20',
};

const content = {
    marginTop: 'inc20',
};

const button = {
    marginTop: 'inc30',
};

const styles = {
    title,
    header,
    widget,
    button,
    content,
};

export default styles;
