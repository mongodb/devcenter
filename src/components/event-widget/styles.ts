import { ThemeUICSSObject } from 'theme-ui';

const header = {
    display: 'flex',
    alignItems: 'center',
};

const widget = {
    marginBottom: 'inc50',
};

const title = {
    marginLeft: 'inc20',
};

const content = {
    display: 'block',
    marginTop: 'inc20',
};

const button: ThemeUICSSObject = {
    display: 'block',
    width: 'auto',
    textAlign: 'center',
    marginTop: 'inc30',
};

const styles = {
    title,
    header,
    widget,
    button,
    content,
};

export default styles;
